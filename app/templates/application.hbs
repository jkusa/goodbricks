<header class="navi-app-header">
  {{top-bar}}
  {{#each flashMessages.queue as |flash|}}
    {{flash-message flash=flash}}
  {{/each}}
</header>
<div class="navi-app-container" id="navi-root">
  {{outlet}}
</div>